import React from 'react';
import '../App.css';

export class Footer extends React.Component { // extends from and uses the code from react

    render(){
        return( // outputs the heading text for the footer to the screen
            <div className="App">
                <h1>My Footer is another component</h1>
            </div>    
        );
    }


}